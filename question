#include<time.h>
#include<bits/stdc++.h>

using namespace std;

int main()
{
    long long  n=0;
    long initialTime=clock();
    while(true)
    {
        ++n;
        if((clock()-initialTime)/CLOCKS_PER_SEC>=60)
        break;

    }
    cout<<n;
}
////////the output gets displayed after 1 min///////////////
